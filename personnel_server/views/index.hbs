<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap');
            /* Основные стили */
            body {
                font-family: Arial, sans-serif;
                background-color: #1c1c1c;
                color: #fff;
                padding: 20px;
            }

            h1 {
                text-align: center;
                color: #00ff7f; /* Neon Green */
            }

            ul {
                list-style-type: none;
                padding: 0;
            }

            li {
                margin: 10px 0;
            }

            /* Статус "up" - Неоново-зеленый */
            .status-up {
                color: #00ff7f;
                text-shadow: 0 0 10px #00ff7f;
            }

            /* Статус "down" - Неоново-красный */
            .status-down {
                color: #ff4500;
                text-shadow: 0 0 10px #ff4500;
            }

            /* Пример для предформатированного текста */
            pre {
                background-color: #2c2c2c;
                padding: 10px;
                border-radius: 5px;
                white-space: pre-wrap;
            }
        </style>
    </head>
    <body>
        <h1>Server Status</h1>
        
        <ul>
            <li><strong>Status:</strong> <span class="{{getStatusClass status}}">{{status}}</span></li>
            <li><strong>Info:</strong>
                <ul>
                    {{#each info}}
                        <li>{{@key}}: <span class="{{getStatusClass this.status}}">{{this.status}}</span></li>
                    {{/each}}
                </ul>
            </li>
            <li><strong>Errors:</strong>
                <ul>
                    {{#each error}}
                        <li>{{@key}}: <span class="{{getStatusClass this.status}}">{{this.status}}</span></li>
                    {{/each}}
                </ul>
            </li>
            <li><strong>Details:</strong>
                <ul>
                    {{#each details}}
                        <li>{{@key}}: <span class="{{getStatusClass this.status}}">{{this.status}}</span></li>
                    {{/each}}
                </ul>
            </li>
        </ul>
    </body>
</html>
